spring.application.name=sprint-service
server.port=8084

# All other config (datasource, Eureka URL, Kafka, JPA) comes from config-server.
# Uses http://localhost:8888 by default; override with SPRING_CLOUD_CONFIG_URI in Docker.
spring.config.import=optional:configserver:
